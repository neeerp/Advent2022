import day0 from '../day0';

describe('On Day 0', () => {
  it('Part 1 is the identity function', () => {
    expect(day0.solvePartOne('hello')).toBe('hello');
  })
});

describe('On Day 0', () => {
  it('Part 2 is the identity function', () => {
    expect(day0.solvePartTwo('hello')).toBe('hello');
  })
});
